import { CostumDirDirective } from './costum-dir.directive';

describe('CostumDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CostumDirDirective();
    expect(directive).toBeTruthy();
  });
});
